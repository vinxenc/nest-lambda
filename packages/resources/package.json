{
  "name": "resources",
  "version": "0.1.0",
  "bin": {
    "resources": "bin/resources.js"
  },
  "scripts": {
    "lint": "eslint \"{cdk,test}/**/*.ts\" --fix",
    "build": "cdk synth",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy --require-approval never"
  }
}